embedding_size: 64
feat_embed_dim: 64
n_mm_layers: 1
n_layers: 2
knn_k: 10
mm_image_weight: 0.1
learning_rate: 0.001
reg_weight: 0.001
dropout: 0
num_layer: 2
hyper_parameters:
- rf_loss_weight
use_rf: true
rf_hidden_dim: 128
rf_n_layers: 2
rf_dropout: 0.1
rf_sampling_steps: 5
rf_learning_rate: 0.0003
rf_loss_weight:
- 0.2
- 0.4
- 0.6
rf_warmup_epochs: 20
rf_mix_ratio: 0
rf_inference_mix_ratio: 0.05
rf_contrast_temp: 0.1
use_2rf: false
use_gradient_checkpointing: true
use_denoise: true
clean_rating_threshold: 5.0
denoise_layers: 2
ps_loss_weight: 0.1
use_wandb: true
wandb_project: GenMMRec_RFCOHESION_SEARCH_microlens
