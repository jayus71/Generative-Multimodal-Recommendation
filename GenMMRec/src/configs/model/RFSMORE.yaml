embedding_size: 64
n_ui_layers: 4
n_layers: 1
learning_rate_scheduler:
- 0.96
- 50
lambda_coeff: 0.9
reg_weight: 0.0001
learning_rate: 0.001
cl_loss: 0.01
temperature: 0.2
image_knn_k: 40
text_knn_k: 15
dropout_rate: 0.1
hyper_parameters:
- rf_learning_rate
use_rf: true
rf_hidden_dim: 128
rf_n_layers: 2
rf_dropout: 0.1
rf_sampling_steps: 10
rf_learning_rate:
- 0.0001
- 0.0003
- 0.0005
rf_loss_weight: 0.4
rf_warmup_epochs: 20
rf_mix_ratio: 0
rf_inference_mix_ratio: 0.05
rf_contrast_temp: 0.1
use_2rf: false
use_gradient_checkpointing: true
use_denoise: true
clean_rating_threshold: 5.0
denoise_layers: 2
ps_loss_weight: 0.1
use_wandb: true
wandb_project: GenMMRec_RFSMORE_SEARCH
